"""lyrics-tool — terminal lyrics visualizer and LRC/WLRC processing suite.

Submodules are imported lazily (import what you need, e.g.
``from lyrics_tool.parser import parse_lrc``) so lightweight commands like
``lyricsooo --help`` don't pull in optional/heavy dependencies.
"""

__version__ = "0.1.0"
